# Railway Environment Variables
# Add these in your Railway project dashboard under "Variables"

NODE_ENV=production
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/notesapp?retryWrites=true&w=majority
JWT_SECRET=generate-with-crypto-randomBytes-32-chars
JWT_REFRESH_SECRET=generate-different-32-char-secret
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d
FRONTEND_URL=https://your-app-name.vercel.app

# Generate secrets with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"